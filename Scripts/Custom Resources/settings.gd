class_name Settings extends Resource

@export var camera_invert_x: bool = false;
@export var camera_invert_y: bool = true;

# Probably need to break this up into separate volume controls
@export var volume: float = 0.5;